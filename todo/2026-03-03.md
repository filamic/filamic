# 2026-03-03

## Testing Scope
- Add/complete model tests for new and related changed models in this branch.
- Add/extend tests for `ProductItemResource`.
- Extend `SupplierResource` tests with unique `ProductItemsRelationManager` behavior.
- Run targeted test files for sanity checks.

## Checklist
- [x] Model tests updated
- [x] ProductItemResource tests added
- [x] SupplierResource relation manager tests added
- [x] Targeted Pest runs passing

## Options/Pluck Cleanup Scope
- Refactor DB-backed Filament `options()` usage to lazy `fn () => ...`.
- Replace `Model::all()/get()->pluck(...)` with `Model::pluck(...)` where accessors/relationship labels are not required.
- Keep `get()/all()` in option builders that require grouped or relationship-derived labels.
- Add permanent coding rule in `claude.md`.

## Options/Pluck Checklist
- [x] `BookFeeInvoicesRelationManager` updated
- [x] `MonthlyFeeInvoicesRelationManager` updated
- [x] `StudentsTable` updated
- [x] `StockMovementForm` options made lazy
- [x] `claude.md` updated with lasting rule

## Order Flow Planning
- [x] Backlog grooming completed for SupplyHub global Purchase Order flow (P0/P1/P2 + test coverage), based on yearly cross-branch procurement requirements.

## SupplyHub Backlog Completed
- [x] [P1] Fix stock race condition in `RecordStockMovement` (lock/atomic decrement).
